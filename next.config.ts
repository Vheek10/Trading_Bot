/**
 * @format
 * @type {import('next').NextConfig}
 */

const nextConfig = {
	images: {
		domains: [
			"cryptologos.cc", // crypto logos
			"pngimg.com", // stars
		],
	},
};

module.exports = nextConfig;
